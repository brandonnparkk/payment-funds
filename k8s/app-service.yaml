apiVersion: v1
kind: Service
metadata:
  name: paymentfunds
  labels:
    app: paymentfunds
spec:
  type: ClusterIP
  selector:
    app: paymentfunds
  ports:
    - name: http
      port: 8080
      targetPort: http
