services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: brandon
      POSTGRES_PASSWORD: devpassword
      POSTGRES_DB: PaymentFunds
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: